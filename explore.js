const name = 'Jolil';

const student = {
    name: 'Jolil',
    age: 40,
    makeMovie: function (name) {
        console.log(name);
    }
}

student.makeMovie(name)

// document.getElementById('name')